代价地图运行流程梳理：

1.声明costmap_interface类型对象global_costmap、local_costmap，并将成员CostmapName_赋值为global_costmap/local_costmap

```
costmapNS::costmap_interface global_costmap("global_costmap");
costmapNS::costmap_interface local_costmap("local_costmap");
```

2. 初始化global_costmap、global_costmap

   ```
   global_costmap.initialize()
   local_costmap.initialize()
   ```

   2.1 读取config/xxxx.yaml配置文件config_file

   ​       在两个文件夹中找：

   ​		1、当前可执行文件处：../config/

   ​		2、/在build同级目录处：./src/costmap2D/config/

   2.2 loadConfigFromYAML(config_file)

